{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Extract Repo Map",
            "type": "debugpy",
            "request": "launch",
            "module": "aider.draft.extract_repo_map",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        },
        {
            "name": "Extract Repo Map 2",
            "type": "debugpy",
            "request": "launch",
            "module": "aider.draft.extract_repo_map_2",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            },
            "args": [
                "D:/github/aider-fork/aider"
            ]
        }
    ]
}